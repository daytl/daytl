import { JwtDecode } from "./JwtDecode";

const tool = {
  name: "jwt-decode",
  github: "https://github.com/daytl/daytl/tree/master/tools/jwt-decode",
  version: "1.0.0",
  component: JwtDecode,
  faqs: [1, 2, 3],
};

export default tool;
